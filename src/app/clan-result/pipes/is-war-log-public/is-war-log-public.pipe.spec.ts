import {IsWarLogPublicPipe} from './is-war-log-public.pipe';

describe('IsWarLogPublicPipe', () => {
  it('create an instance', () => {
    const pipe = new IsWarLogPublicPipe();
    expect(pipe).toBeTruthy();
  });
});
