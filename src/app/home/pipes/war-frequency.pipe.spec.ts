import {WarFrequencyPipe} from './war-frequency.pipe';

describe('WarFrequencyPipe', () => {
  it('create an instance', () => {
    const pipe = new WarFrequencyPipe();
    expect(pipe).toBeTruthy();
  });
});
